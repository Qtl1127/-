interface:
  display_name: "自动化主图"
  short_description: "按提示词和规则批量升级电商主图"
  default_prompt: "Use $automate-main-images to upgrade my product images with the bundled prompt rules and quality-check workflow."
policy:
  allow_implicit_invocation: true
