interface:
  display_name: "自动化主图"
  short_description: "批量优化产品主图为800x800 PNG"
  default_prompt: "Use $automate-main-images to batch optimize product main images from my source folder into a clean output folder."
policy:
  allow_implicit_invocation: true
